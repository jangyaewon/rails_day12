<!--카페에 등록된 글( 카페 정보 X )-->
<h1><%= @cafe.title %></h1>
<h3><%= @cafe.description %></h3>


<ul class="list-group">
  <% @cafe.posts.each do |post| %>
  <li class="list-group-item d-flex justify-content-between align-items-center">
    <%= link_to "#{post.title}", post_path(post) %><!-- 경로에 id가 붙는 건 post를 붙여서 -->
    <span class="badge badge-primary badge-pill"><%=post.comments.count%></span>
  </li>
  <% end  %>
</ul>

<!-- 새글 등록 버튼 -->
<%= link_to "카페가입하기",join_cafe_path(@cafe), method: 'POST' , class: "btn btn-primary" %>
<br><%= link_to '새글쓰기', new_post_path(cafe_id: @cafe.id), class: 'btn btn-info'%>